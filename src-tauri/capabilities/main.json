{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Window permissions for the Time & Flow popover and Settings window.",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "notification:default",
    "autostart:default"
  ]
}
