// Entry points for the two windows. main.tsx mounts `App` for the menubar
// popover (label "main") and `SettingsRoot` for the Settings window
// (label "settings").
export { App } from "./Popover";
export { SettingsRoot } from "./Settings";
